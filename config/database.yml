development:
  adapter: postgresql
  database: echoes_development
  host: localhost

test:
  adapter: postgresql
  database: echoes_test
  host: localhost

# production:
#   adapter: postgresql
#   database: echoes_production
#   host: localhost

default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5